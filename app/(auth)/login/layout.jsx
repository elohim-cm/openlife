export const metadata = {
  title: 'OpenLife | Connexion',
  description: "Portail de connexion de l'application Web OpenLife",
};

const LoginLayout = ({children}) => children;

export default LoginLayout;
